<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bmi">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".extremeObesity" />
        <activity android:name=".obesityClass2" />
        <activity android:name=".obesityClass1" />
        <activity android:name=".overWeight" />
        <activity android:name=".normalWeight" />
        <activity android:name=".underweight" />
        <!--<activity android:name=".bmiResults" />-->
        <activity android:name=".splashEntry">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>